/**
 * Dark mode is not supported — this starter is light-mode only. Kept as a
 * stub (rendering nothing) so nothing importing `ThemeScript` breaks.
 */
export function ThemeScript() {
  return null;
}
